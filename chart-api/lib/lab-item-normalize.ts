/**
 * Re-export from chart-app to avoid duplication.
 * Both `lib/lab-item-normalize.ts` and `lib/chart-app/lab-item-normalize.ts` are identical.
 */
export { canonicalizeLabItemName, type LabCanonicalizeSpecies } from '@/lib/chart-app/lab-item-normalize';
