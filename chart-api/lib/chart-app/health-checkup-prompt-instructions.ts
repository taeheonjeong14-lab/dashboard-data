import {
  HEALTH_CHECKUP_MAX_RECHECK_BODY_CHARS,
  HEALTH_CHECKUP_MAX_RECHECK_TITLE_CHARS,
  HEALTH_CHECKUP_PROMPT_DENTAL_SKIN_DX_MAX_CHARS,
  HEALTH_CHECKUP_PROMPT_DENTAL_SKIN_IMP_MAX_CHARS,
  HEALTH_CHECKUP_PROMPT_IMAGING_INTERP_MAX_CHARS,
  HEALTH_CHECKUP_PROMPT_LAB_INTERP_MAX_CHARS,
  HEALTH_CHECKUP_PROMPT_MAX_FOLLOW_UP_CHARS,
  HEALTH_CHECKUP_PROMPT_MAX_OVERALL_CHARS,
  HEALTH_CHECKUP_PROMPT_MIN_FOLLOW_UP_CHARS,
  HEALTH_CHECKUP_PROMPT_MIN_OVERALL_CHARS,
  HEALTH_CHECKUP_PROMPT_SYSTEMS_DX_MAX_CHARS,
  HEALTH_CHECKUP_PROMPT_SYSTEMS_IMP_MAX_CHARS,
} from '@/lib/chart-app/health-checkup-limits';
import { HEALTH_CHECKUP_SYSTEMS_LLM_FIELD_KEYS } from '@/lib/chart-app/health-checkup-systems-llm-merge';

// ──────────────────────────────────────────────────────────────────────────
// 공용 프롬프트 블록 — 전체 생성(buildHealthCheckupInstructionBody)과
// 섹션 재생성(buildSectionPrompt)이 **둘 다** 가져다 쓴다. 한 곳만 고치면 양쪽에 반영된다.
// ──────────────────────────────────────────────────────────────────────────

/** 근거 고정(환각 방지) 규칙. */
export const HEALTH_CHECKUP_GROUNDING_LINES: string[] = [
  '========== 절대 규칙: 근거에 없는 내용 금지 (최우선) ==========',
  '- 아래 「참고 데이터」에 **명시적으로 존재하는 사실만** 사용한다. 데이터에 없는 수치·검사 결과·신체검사 소견·병명·영상 판독을 **추측하거나 만들어내지 않는다.**',
  '- 확실하지 않으면 쓰지 않는다. **내용을 빠뜨리는 것이 지어내는 것보다 항상 낫다.**',
  '- 뒤에 나오는 「빠짐없이 반영」·「완전성」 규칙은 **데이터에 실제로 존재하는 소견에만** 적용된다. 근거가 없으면 장기·검사 칸은 고정 문구를 쓰고, 종합 소견·사후 관리에는 **아예 언급하지 않는다.**',
  '- 각 장기 섹션에 나열된 질환 목록은 **확인용 예시일 뿐**이다. 데이터에서 진단·의심된 경우에만 해당 질환을 쓰고, **근거가 없는 질환은 어떤 칸에도 쓰지 않는다.**',
  '- 수치는 참고 데이터에 **나온 값을 그대로만** 인용한다. 값을 추정·반올림·창작하지 않으며, 항목명·단위도 데이터와 일치시킨다.',
];

/** 보호자 대상 호칭·말투·전문용어 표기 규칙(이름만 호명 / ~해 주세요 / 보호자님은 / 국내 교과서 표준 용어 · 혈액지표 외 한글만 · 의약품명 뭉뚱그리기). */
export const HEALTH_CHECKUP_OWNER_TONE_LINES: string[] = [
  '- 환자를 부를 때 **「OO 환자는」처럼 "환자"를 붙이지 않는다.** 이름만 써서 **「OO는/은」** 으로 표현한다. (예: "초코 환자는" → "초코는")',
  '- **「~해 주십시오」체는 쓰지 않는다. 「~해 주세요」** 로 쓴다.',
  '- 보호자를 가리킬 때는 **「보호자는」이 아니라 「보호자님은」** 처럼 "보호자님"으로 높여 쓴다.',
  '- 앞으로 보호자가 해야 할 일은 **요청·권유 말투**로 쓴다 — 「~해 주세요」, 「~을 권장드립니다」, 「~이 필요합니다」. **「~하기로 하셨습니다 / ~해 주기로 했습니다」처럼 보호자가 이미 동의·약속한 것처럼 단정하는 표현은 쓰지 않는다.** (예: "체중을 관리해 주기로 하셨습니다" ✗ → "체중 관리를 권장드립니다" / "꾸준히 관리해 주세요" ○)',
  '- 의학 전문용어·질환명은 **반드시 국내(한국) 수의학 교과서에서 실제로 쓰는 표준 용어**를 그대로 사용한다. 외국 교과서 용어를 기계적으로 직역한 어색한 한국어(억지 번역투)는 쓰지 않는다.',
  '- **혈액검사 지표·약어(예: ALT, BUN, ALP, MMVD 판정에 쓰는 수치 등)를 제외하고는 영어를 괄호로 병기하지 않고 한글 용어로만 쓴다.** (예: "만성 신장병(CKD)" → "만성 신장병", "승모판 폐쇄부전(MMVD)" → "승모판 폐쇄부전") 혈액검사 지표처럼 통용되는 약어만 영어 그대로 쓸 수 있다.',
  '- **구체적인 의약품명·성분명(예: 실리마린, UDCA, 엔로플록사신 등)은 보호자가 이해하기 어려우므로 쓰지 않는다.** 대신 「처방드린 약」처럼 뭉뚱그려 쓰거나, 조금 더 구체화가 필요하면 「항생제」·「소염제」·「간 보호제」처럼 **약효 분류(효능군) 수준**으로만 표현한다. (특히 사후 관리)',
  '- **[모든 섹션 공통] 한글 조사는 앞 단어의 받침(끝소리 자음) 유무에 맞춰 정확히** 쓴다 — 은/는, 이/가, 을/를, 와/과, (으)로, (이)란 등. (예: 받침 있음 → "결석이·심장은·소견을", 받침 없음 → "슬러지가·위는·증세를"; "담낭슬러지란?"○ "담낭슬러지이란?"✗)',
  '- 환자 이름을 문장에서 부를 때: **받침으로 끝나는 이름이면 반드시 이름 뒤에 "이"를 붙여** 자연스럽게 쓴다. (예: "버들" → 버들이는 / 버들이가 / 버들이의 / 버들이를) 받침 없는 이름은 그대로 쓴다. (예: "보리" → 보리는 / 보리가 / 보리의) 이 규칙은 **종합소견·사후관리·권장 재검진·장기 섹션 등 모든 곳**에 적용한다.',
];

/** 종합 소견 작성 규칙(오프닝 2시나리오 + 번호 형식). 헤더·글자수 줄은 호출부에서 붙인다. */
export const HEALTH_CHECKUP_OVERALL_RULE_LINES: string[] = [
  '- 맨 앞에 검진 전반을 요약하는 **오프닝 한 문장**을 먼저 쓰고, **빈 줄 한 줄**을 둔 뒤 번호 목록을 시작한다. 오프닝은 검진 결과에 따라 아래 둘 중 하나를 골라 쓰되, **〈환자 이름〉은 참고 데이터의 실제 값**으로 채운다(없으면 "반려동물"로, 지어내지 않음):',
  '  · 큰 이상이 없거나 경미할 때 → "이번 건강검진 결과, 〈환자 이름〉의 건강은 전반적으로 큰 이상 없이 양호한 상태로 확인되었습니다."',
  '  · 몇 군데 주의 깊게 살펴봐야 할 소견이 있을 때 → "이번 건강검진에서 〈환자 이름〉의 건강 상 몇 가지 주의 깊게 살펴봐야 할 소견이 확인되었습니다."',
  '- 이번 검진을 통해 진단 또는 의심할 수 있는 내용을, **큰 단위의 주제(질환·소견)별로 번호를 매겨** 정리한다.',
  '- **형식(엄격히 지킬 것)**: 각 항목은 「번호. 주제명」 한 줄 → 줄바꿈(\\n) → 그 주제를 **딱 한 문장**으로 요약한 본문 한 줄. 항목과 항목 사이에는 **빈 줄 한 줄**을 둔다. 예시:',
  '  1. 신장 결석',
  '  좌측 신장에 결석 소견이 관찰되어 경과 관찰이 필요합니다.',
  '',
  '  2. 방광벽 비후',
  '  방광벽이 두꺼워져 방광염 가능성이 있어 추가 확인이 필요합니다.',
  '- 주제명은 짧은 명사구(질환/소견명)로 쓴다. 본문은 핵심 근거를 담되 **한 문장**으로만 쓰고, 여러 문장으로 늘리지 않는다.',
  '- 각 항목 본문은 **이번 검진에서 무엇이 발견되었는지(소견)와 그것이 무엇을 의미하는지·어떤 위험이 있는지**를 설명한다. **앞으로의 조치·관리 계획(「~해 주세요」, 재검·투약 안내 등)은 종합 소견에 쓰지 않는다 — 그건 사후 관리에서 다룬다.**',
  '- ★**[종합 소견은 「질환 단위」로 쓴다 — 수치 단위로 쓰지 않는다]** 개별 검사 수치의 높낮이를 소견으로 삼지 않는다. 그 수치를 해석해서 나오는 **임상적 결론(질환·병태)** 이 있을 때만, 그 결론을 주제로 삼아 항목을 쓴다.',
  '  · 나쁜 예: "cPL 수치가 높습니다", "ALT가 120 U/L (정상범위 12 - 101)로 상승했습니다" — 수치의 높낮이가 주제가 되어 있다.',
  '  · 좋은 예: "췌장 염증 가능성" / "췌장의 염증 소견이 의심되어 추가 확인이 필요합니다." — 해석해서 나온 결론이 주제다.',
  '  · **종합 소견 본문에는 검사 항목명·측정값·단위·정상범위를 쓰지 않는다.** ("전 섹션 공통" 의 혈액검사 수치 표기 규칙은 종합 소견에는 적용하지 않는다 — 수치와 근거 제시는 혈액검사 해석·장기 칸에서 다룬다.)',
  '  · 수치가 이상하더라도 **거기서 끌어낼 만한 임상적 결론이 없으면 종합 소견에는 넣지 않는다.** (그 항목은 혈액검사 해석 칸에서 다뤄지므로 리포트에서 사라지는 것이 아니다.)',
  '  · 검진 포인트가 "간 수치 상승(ALT 120)" 처럼 수치 형태로 적혀 있어도, 종합 소견에는 그것이 시사하는 결론(예: "간 효소 상승 — 간담도계 이상 의심")으로 바꿔 쓴다.',
  '- 차트 본문에서 의심된다고 언급된 질병은 크던 작던 반드시 한 항목(번호)으로 포함한다.',
  '- 번호 순서는 **심각도 순** — 가장 심각한 것이 1번, 가장 덜 심각한 것이 마지막.',
];

/** 사후 관리 작성 규칙(오프닝 2시나리오 + 번호 형식 + 엔딩). 헤더·글자수 줄은 호출부에서 붙인다. */
export const HEALTH_CHECKUP_FOLLOWUP_RULE_LINES: string[] = [
  '- 맨 앞에 **오프닝 한 문장**을 먼저 쓰고, **빈 줄 한 줄**을 둔 뒤 번호 목록을 시작한다. 오프닝은 검진 결과에 따라 아래 둘 중 하나를 골라 쓰되, **〈환자 이름〉은 참고 데이터의 실제 값**으로 채운다(없으면 "반려동물"로, 지어내지 않음):',
  '  · 큰 이상이 없거나 경미할 때 → "〈환자 이름〉은/는 시급한 조치는 없으나 아래 내용을 참고해 평소처럼 건강하게 관리해 주세요."',
  '  · 몇 군데 주의 깊게 살펴봐야 할 소견이 있을 때 → "확인된 소견에 따라 〈환자 이름〉의 건강 관리 계획을 안내드리니, 꼼꼼히 살펴보시고 귀가 후에도 보호자님께서 신경 써 관리해 주세요."',
  '- 종합 소견의 각 주제(질환·소견)에 대한 앞으로의 조치·대응 계획을, **종합 소견과 동일한 번호·주제 체계로** 번호를 매겨 정리한다.',
  '- **형식(엄격히 지킬 것)**: 각 항목은 「번호. 주제명」 한 줄 → 줄바꿈(\\n) → 그 주제의 관리 계획을 **딱 한 문장**으로. 항목 사이 **빈 줄 한 줄**. (번호·주제명은 종합 소견과 일치시킨다)',
  '- 종합 소견에 있는 주제는 **하나도 빠뜨리지 말고** 각각 대응 계획을 한 문장으로 쓴다.',
  '- 반대로 **종합 소견에 없는 주제를 사후 관리에서 새로 만들지 않는다.** 항목 집합은 종합 소견과 정확히 같아야 한다(개수·순서·주제명 일치).',
  '  · 특히 **검사 수치만으로는 임상적 결론이 없어 종합 소견에서 뺀 항목을 사후 관리에서 되살리지 않는다.** (예: 종합 소견에 없는데 사후 관리에만 "ALT 재검" 이 생기는 것 — 금지)',
  '  · 종합 소견 항목이 질환 단위로 적혀 있으므로, 사후 관리 본문에서는 필요하면 **검사 수치·항목명을 근거로 써도 된다**(수치 표기 규칙은 사후 관리에 그대로 적용된다). 다만 주제·번호는 종합 소견을 따른다.',
  '- 보호자가 집에서 해줘야 할 일이 있으면 해당 주제 문장에 함께 담는다.',
  '- 번호 목록 뒤에 **빈 줄 한 줄**을 두고, 마지막에 **엔딩 한 문장**을 아래 양식으로 붙인다 (〈병원명〉·〈환자 이름〉은 참고 데이터의 실제 값을 채운다):',
  '  → "앞으로도 저희 〈병원명〉이 〈환자 이름〉의 건강을 꼼꼼히 살피겠습니다. 평소와 다른 점이 보이면 언제든 편하게 내원해 주세요."',
  '  · 병원명이 데이터에 없으면 "저희 병원"으로, 환자 이름이 데이터에 없으면 "반려동물"로 대체한다. (이름·병원명을 임의로 지어내지 않는다)',
];

/**
 * 장기별 **필수 점검 항목**. 검진 데이터로 확인되면 정상/이상 여부를 주요 진단(_dx)에 짧게라도 반드시 언급.
 * 단, 검진으로 확인할 수 없는 항목은 언급하지 않는다(지어내지 않음).
 * 키는 hp* 섹션 키(circ/digest/endo/renal_uro/hepatobiliary/msk/dental/skin).
 */
export const HEALTH_CHECKUP_REQUIRED_CHECK_ITEMS: Record<string, string[]> = {
  circ: ['심잡음', 'VHS', '호흡 패턴', '폐 상태'],
  digest: ['식욕', '변 상태', '간수치', '췌장수치'],
  endo: ['음수량 변화', '소변량 변화', '혈당', '콜레스테롤', '전해질', 'ACTH'],
  renal_uro: ['BUN', 'Creatinine', 'SDMA', '결석', '방광염', '혈압', '혈뇨'],
  hepatobiliary: ['간수치(ALT·AST·ALP·GGT)', '담낭 슬러지'],
  msk: ['보행 상태', '통증'],
  dental: ['(치과) 치석', '(치과) 치은염', '(안과) 각막·수정체 혼탁', '(안과) 안압'],
  skin: ['가려움', '분비물', '냄새'],
};

/** 장기 블록에 넣을 "필수 점검 항목" 지시 한 줄. 항목이 없으면 빈 문자열. */
export function healthCheckupRequiredCheckItemsLine(key: string): string {
  const items = HEALTH_CHECKUP_REQUIRED_CHECK_ITEMS[key];
  if (!items || items.length === 0) return '';
  return (
    `- **필수 점검 항목**: ${items.join(' · ')} — 검진 데이터로 확인되는 항목은 (정상이든 이상이든) 주요 진단에 **짧게라도 반드시 언급**한다. ` +
    '단, **검진으로 확인할 수 없는 항목은 언급하지 않는다**(값·소견을 지어내지 않음).'
  );
}

/** 주요 진단(_dx)·시사점(_imp)의 역할 정의. 장기·검사 섹션 공통(전체/섹션 공유). */
export const HEALTH_CHECKUP_DX_IMP_GUIDE_LINES: string[] = [
  '· 주요 진단(_dx): 이번 검진에서 **발견·관찰된 내용(팩트)** 을 설명한다. (예: "심장 크기가 정상보다 증가하였습니다.")',
  '· 주요 진단(_dx)의 **표현 수위는 input 자료의 근거에 따라 아래 3가지로 명확히 구분**한다. 미포함과 정상소견을 한 문장에 묶어 "~는 포함되지 않았거나 특이 소견이 확인되지 않았습니다" 처럼 어중간하게 쓰지 말 것.',
  '  ① 해당 질환의 유무를 판단할 근거가 input 자료에 **하나도 없으면** → 그 질환은 언급하지 않는다(만들어내지 않는다). 섹션 전체에 연결할 근거가 전혀 없을 때만 미포함 고정 문구를 쓴다.',
  '  ② input 자료에 **확정 진단이 명확히 내려진 내용만** "~가 확인되었습니다" 같은 단정 표현을 쓴다.',
  '  ③ 근거(검사 수치·영상·관찰 등)는 있으나 input 자료에 확정 진단 문구가 없으면 → "~를 추측해볼 수 있습니다 / ~의 가능성이 있습니다" 처럼 단정하지 않는 표현으로 쓴다.',
  '· ★**안 한 항목은 언급하지 않는다(매우 중요)**: 이번 검진에 포함되지 않았거나 시행되지 않은 개별 검사·항목을 "○○는 이번 검진에 포함되지 않았습니다 / 시행되지 않았습니다" 처럼 **단순히 나열·언급하지 않는다.** 실제로 시행·확인된 내용만 서술하고, 안 한 항목은 아예 적지 않고 넘어간다. (그 계통에 서술할 실제 소견이 하나라도 있으면, 나머지 안 한 항목의 미포함 언급은 붙이지 않는다. **계통 전체에 연결할 근거가 하나도 없을 때에만** 미포함 고정 문구 한 줄을 쓴다.)',
  '  · 단, **예외**: 시행한 검사에서 이상·의심 소견이 있어 추가 확인이 필요하면, **시사점(_imp)에서 "더 정확한 확인을 위해 ○○ 검사를 추가로 받아보시길 권장드립니다" 처럼 후속(추가) 검사를 권고하는 것은 오히려 좋다.** 이는 단순 미포함 나열과 다르다(소견에 근거한 능동적 권고). 단, 소견이 정상이거나 근거가 없으면 억지로 추가 검사를 권하지 않는다.',
  '· 시사점(_imp): 그 소견이 **무엇을 의미하는지, 앞으로 어떤 위험이 있는지** 자세히 설명하고, 더해서 **언급된 질환을 앞으로 어떻게 대응·관리해야 하는지**까지 담는다. (예: "심부전이 의심되며, 진행 시 기침·호흡곤란 위험이 있어 정기적인 심장 검사와 약물 관리가 필요합니다.")',
  '· **증상 장기 ↔ 원인 장기 분리(중요)**: 어떤 증상이 나타난 장기와 그 증상의 **근본 원인이 되는 장기가 다를 수 있다.** 질환의 본체(병변명·진단·자세한 설명)는 반드시 그 질환이 **해부학적으로 귀속되는 장기 섹션**에 쓴다. 증상만 나타난 다른 장기 섹션에서는 그 질환을 자세히 서술하지 말고, 「이 증상의 원인은 ○○(원인 장기) 문제일 가능성이 높으며, 본 계통 자체는 전반적으로 양호하다」는 식으로 원인을 해당 섹션으로 넘긴다.',
  '  (예: 고양이가 사료 섭취 후 구토 → 원인이 치아 흡수성 병변일 때: **소화기(digest)**에는 "구토는 소화기 자체 문제보다 치아 문제일 가능성이 높고, 소화기 건강은 전반적으로 양호합니다"로 쓰고, **치아 흡수성 병변의 자세한 설명은 「치과 및 안과(dental)」 섹션**에 쓴다.)',
];

/**
 * 검사 수치 해석 규칙 — **리포트 전반(장기·영상·혈액검사 등 모든 칸) 공통**.
 * 전체 생성과 섹션 재생성 양쪽 공통 헤더에 끼워 쓴다.
 */
export const HEALTH_CHECKUP_VALUE_INTERPRETATION_LINES: string[] = [
  '- **[리포트 전반 공통] 검사 수치·지표를 언급하면 반드시 「그 값이 무엇을 뜻하는지」 해석을 함께 쓴다.** 숫자만 던지고 끝내지 않는다. 보호자(비전공자)가 그 수치가 정상인지·높은지·낮은지, 그래서 임상적으로 무엇을 의미하는지 알 수 있어야 한다.',
  '- **[측정 대상·의미 명시 · 전 섹션 공통] 어떤 검사 수치·등급·점수든(혈액값·영상 지표뿐 아니라 슬개골 탈구 0/4 같은 등급 척도 포함) 처음 언급할 때 「무엇을 측정·평가한 값이고 어떤 척도인지」와 「그래서 무슨 의미인지」를 아주 간략하게라도 반드시 함께 쓴다.** (예: "슬개골 탈구는 0~4단계로 평가하는데, 좌·우 모두 0/4로 탈구 소견이 없는 정상입니다." — 척도·의미 설명 없이 "슬개골 0/4"처럼 숫자만 쓰지 않는다.)',
  '  · 권장 형식: 「(그 지표가 무엇을 재는 값인지 한 줄 풀이) + 측정값 + (정상/증가/감소 등 판정) + (그래서 의미하는 바)」.',
  '  · 나쁜 예: "VHS가 7로 측정되었습니다." → 크다는 건지 작다는 건지 정상인지 알 수 없다.',
  '  · 좋은 예: "심장 크기를 나타내는 지표인 VHS가 7로 측정되어, 심장이 비대해지지 않은 정상 범위의 크기로 확인되었습니다." 처럼 값이 정상인지·이상인지와 그 의미를 함께 쓴다.',
  '  · 단, 정상/이상 판정과 해석은 일반적으로 통용되는 수준에서만 하고, **참고 데이터에 없는 구체 기준 수치(정상 범위 등)를 단정적으로 지어내지 않는다.** 기준이 불확실하면 구체 범위를 만들지 말고 정상/이상 여부와 그 의미를 말로 풀어 설명한다.',
  '- **[혈액검사 수치 표기 · 종합 소견 제외한 전 섹션 공통] 혈액검사 결과 수치를 언급할 때는 반드시 「측정값 + 단위」를 쓰고, 바로 옆 괄호 안에 「정상범위」를 함께 쓴다.** (★종합 소견에는 수치 자체를 쓰지 않는다 — 종합 소견 규칙 참조. 이 표기 규칙은 종합 소견에는 적용하지 않는다.) 형식: `측정값단위 (정상범위 하한 - 상한)`. (예: "ALT가 120 U/L (정상범위 12 - 101)로 상승", "BUN 18 mg/dL (정상범위 7 - 29)") 정상범위는 **참고 데이터의 각 항목 `ref` 값을 그대로** 쓰고, `ref` 가 없는 항목은 정상범위 괄호를 생략한다(범위를 지어내지 않는다).',
  '- **[BCS(체형 점수) 판정 기준 · 전 섹션 공통] 9점 척도 BCS 는 「7점 이상」부터 과체중으로 쓴다.** **6점은 「과체중」·「비만」으로 단정하지 않는다** — 6점은 "정상과 과체중의 경계에 있다" 정도로 쓴다. (4~5점 = 이상적, 6점 = 정상과 과체중의 경계, 7점 이상 = 과체중, 8~9점 = 비만)',
  '  · 참고 데이터의 BCS 가 9점 척도가 아니면(예: 5점 척도 "BCS 3/5") 위 숫자 기준을 그대로 적용하지 않는다. 척도가 무엇인지 함께 밝히고(위 「측정 대상·의미 명시」 규칙) 그 척도 기준으로 해석한다.',
  '- **[리포트 전반 공통] 막연한 이상 단정 금지.** "비정상적인 구조가 관찰되었습니다", "이상 소견이 있습니다", "추가적인 평가가 필요합니다" 처럼 **무엇이·어디가·어떻게 이상한지 없이 두루뭉술하게 이상을 단정하는 표현을 절대 쓰지 않는다.**',
  '  · 이상 소견을 쓸 거면 반드시 (1) 어느 부위·구조가 (2) 어떻게(크기·모양·음영·위치·수치 등 구체적 근거) (3) 그래서 임상적으로 무슨 의미인지 — 이 세 가지를 근거와 함께 적는다.',
  '  · **참고 데이터에서 그만큼 구체적인 근거를 찾을 수 없으면, 이상하다고 막연히 적지 말고** 해당 항목을 근거와 함께 정상 소견으로 서술하거나(예: "~수치가 정상 범위로 확인되었습니다"), 섹션 전체에 연결할 근거가 전혀 없으면 미포함 고정 문구를 쓴다. **막연한 이상 단정보다 "정상" 또는 "미포함"이 항상 낫다.**',
];

/**
 * 질환 소개 박스(3·4p) 생성 규칙. 전체/섹션 프롬프트 공유.
 * 페이지: 3p = 순환기(circ)·소화기(digest)·내분비(endo), 4p = 신장비뇨(renal_uro)·간담도(hepatobiliary)·근골격(msk).
 */
export const HEALTH_CHECKUP_DISEASE_BOX_RULE_LINES: string[] = [
  '· 질환 소개 후보(선택): 각 장기에 대해 **input 자료에서 확진(확정 진단)된 질환** 중 보호자에게 소개할 만한 질환들의 **이름만** 문자열 배열로 출력한다. **본문은 쓰지 않는다**(직원이 선택 시 별도로 생성).',
  '· 확진이 아니거나(추측·가능성 수준이거나) 근거가 없으면 그 장기 배열은 **빈 배열**로 둔다. 없는 질환을 만들어내지 않는다.',
  '· 질환명은 한국어 표준 용어로 간결하게 적는다. (예: "이첨판 폐쇄부전증(MMVD)", "쿠싱증후군", "슬개골 탈구")',
];

/** 장기 섹션 순서(전체 생성에서 이 순서로 출력). */
export const HEALTH_CHECKUP_ORGAN_ORDER = [
  'circ', 'digest', 'endo', 'renal_uro', 'hepatobiliary', 'msk', 'dental', 'skin',
] as const;

type HealthCheckupOrganTier = 'systems' | 'dentalSkin';

/** 장기별 스펙(제목·JSON키·관련 질환·인용 검사·글자수 티어). 전체/섹션 프롬프트가 공유. */
export const HEALTH_CHECKUP_ORGAN_SPECS: Record<
  string,
  { title: string; keys: string; diseases: string; citedTests: string; tier: HealthCheckupOrganTier }
> = {
  circ: {
    title: '순환기&호흡기', keys: 'hp3_circ_dx / hp3_circ_imp', tier: 'dentalSkin',
    diseases: '심장병, 심비대, 폐렴, 기관지염, 기관허탈, 폐수종, 흉수, 폐 종괴 등 순환기·호흡기 관련 질환',
    citedTests: '심장 크기, 폐 음영 증가 여부, 기관 협착, 흉수 여부, 심잡음, 호흡 패턴 이상 등',
  },
  digest: {
    title: '소화기', keys: 'hp3_digest_dx / hp3_digest_imp', tier: 'dentalSkin',
    diseases: '위염, 장염, 위장관 이물, 췌장염, 장 비후, 장 운동성 저하, 장 종괴 등 소화기 관련 질환',
    citedTests: '췌장염 수치, 단백 수치 변화, 위장관 비후, 장 운동성 감소, 장내 가스, 복강 내 이물 등',
  },
  endo: {
    title: '내분비계', keys: 'hp3_endo_dx / hp3_endo_imp', tier: 'systems',
    diseases: '당뇨병, 갑상선 기능 이상, 쿠싱증후군, 에디슨병, 비만 관련 대사 이상 등 내분비계 관련 질환',
    citedTests: '혈당, 콜레스테롤, 중성지방, 호르몬 수치, 다음다뇨 여부, 식욕 변화 등',
  },
  renal_uro: {
    title: '신장 및 비뇨기계', keys: 'hp3_renal_uro_dx / hp3_renal_uro_imp', tier: 'systems',
    diseases: '신부전, 신장 결석, 방광 결석, 방광염, 요로 감염, 요로 확장, 단백뇨 등 신장·비뇨기계 관련 질환',
    citedTests: 'BUN, Creatinine, SDMA, 전해질 이상, 단백뇨, 혈뇨, 요비중 이상, 신장 크기·구조 변화, 결석, 방광벽 비후 등',
  },
  hepatobiliary: {
    title: '간담도계', keys: 'hp3_hepatobiliary_dx / hp3_hepatobiliary_imp', tier: 'systems',
    diseases: '간염, 지방간, 간 기능 저하, 담낭 슬러지, 담낭 점액종, 담석, 담관 폐색, 간 종괴 등 간담도계 관련 질환',
    citedTests: 'ALT, AST, ALKP, GGT, 빌리루빈, 알부민, 간비대, 간 종괴, 담낭 슬러지, 담관 확장 여부 등',
  },
  msk: {
    title: '근골격계', keys: 'hp3_msk_dx / hp3_msk_imp', tier: 'systems',
    diseases: '관절염, 슬개골 탈구, 십자인대 손상, 고관절 이형성, 척추 질환, 디스크 질환, 골절, 근육 손상 등 근골격계 관련 질환',
    citedTests: '관절 변형 여부, 관절 간격 감소, 골 증식, 골절 여부, 척추 정렬 이상 등',
  },
  dental: {
    title: '치과 및 안과', keys: 'hp4_dental_dx / hp4_dental_imp', tier: 'dentalSkin',
    diseases:
      '(치과) 치석, 치주염, 치은염, 치아 흔들림, 치아 파절, 치근 농양, 구내염, 구강 종괴 등 / (안과) 각막·수정체 혼탁, 백내장, 핵경화, 각막궤양, 결막염, 녹내장(안압 상승) 등 — **안과 검사·소견이 데이터에 있을 때만**',
    citedTests: '치석 정도, 잇몸 염증, 치아 흔들림·파절, 구취 / (안과) 혼탁·안압 등',
  },
  skin: {
    title: '피부·외이도', keys: 'hp4_skin_dx / hp4_skin_imp', tier: 'dentalSkin',
    diseases:
      '(피부) 알레르기성 피부질환, 세균성 피부염, 진균성 피부염, 탈모, 피부 종괴, 습진 등 / (외이도, 주로 강아지) 외이염, 귀 진드기 감염, 외이도 협착, 귀 분비물, 귀 냄새 증가 등',
    citedTests: '피부 상태, 귀 검사 결과 등 관련 소견',
  },
};

/** 장기 한 칸 블록(헤더 + 관련 질환 + 인용 검사 + 필수 점검 항목 + 고정문구 + 글자수). 전체/섹션 공유. */
export function healthCheckupOrganBlockLines(key: string): string[] {
  const s = HEALTH_CHECKUP_ORGAN_SPECS[key];
  if (!s) return [];
  const dxMax = s.tier === 'dentalSkin' ? HEALTH_CHECKUP_PROMPT_DENTAL_SKIN_DX_MAX_CHARS : HEALTH_CHECKUP_PROMPT_SYSTEMS_DX_MAX_CHARS;
  const impMax = s.tier === 'dentalSkin' ? HEALTH_CHECKUP_PROMPT_DENTAL_SKIN_IMP_MAX_CHARS : HEALTH_CHECKUP_PROMPT_SYSTEMS_IMP_MAX_CHARS;
  return [
    `========== ${s.title} (${s.keys}) ==========`,
    `- 관련 질환: ${s.diseases}`,
    `- 인용 검사(데이터에 있으면 인용): ${s.citedTests}`,
    healthCheckupRequiredCheckItemsLine(key),
    '- **주요 진단에 검사 수치를 쓰면 숫자만 적지 말고, 그 값이 정상인지·이상인지와 무슨 의미인지를 반드시 짧게 한마디 덧붙인다.**',
    '- 관련 근거가 없으면 위 고정 문구를 사용한다.',
    '- ★고정 문구(미포함 영역)를 쓸 때는 **주요 진단·시사점 두 칸 모두** 같은 문구를 쓴다. 한쪽만 미포함이라 해놓고 다른 칸에 소견을 적으면 앞뒤가 안 맞는다.',
    `- 주요 진단 최대 ${dxMax}자, 시사점 최대 ${impMax}자`,
  ];
}

/** 영상(방사선·초음파) 블록. 전체/섹션 공유. */
export function healthCheckupImagingBlockLines(): string[] {
  const imgMax = HEALTH_CHECKUP_PROMPT_IMAGING_INTERP_MAX_CHARS;
  const modality = (titleKo: string, key: string): string[] => [
    `========== ${titleKo} (${key}) ==========`,
    `- 케이스에 첨부된 ${titleKo} 이미지를 판독하여 작성한다.`,
    `- ★이미지가 첨부되지 않았더라도, 차트·종합소견에 ${titleKo} 판독 결과가 적혀 있으면(예: 슬개골 탈구·고관절 이형성증 등 정형 방사선 소견) 그 내용을 근거로 소견을 작성한다. 사진이 없다는 이유로 고정 문구를 쓰지 말 것.`,
    `- 고정 문구는 이 ${titleKo} 검사를 **실제로 하지 않았을 때**(차트에도 판독 결과가 전혀 없을 때)만 쓴다.`,
    '- 포커스는 질병보다 **사진 상의 특이점** 위주. 어느 부위 이미지의 어느 부분에서 어떤 특이점이 보이는지 구체적으로 쓴다.',
    '- 보호자가 이해할 수 있도록 풀어서 설명도 함께 포함한다.',
    '- **제공된 영상 분석 데이터에 기술된 소견만 사용한다. 데이터에 없는 위치·특이점을 지어내지 않는다.** 영상 근거가 없거나 특이점이 없으면 고정 문구를 사용한다.',
    '- **안 한 것은 언급하지 않는다**: 이번 검진에 포함되지 않았거나 촬영/검사하지 않은 부위·항목을 "○○는 포함되지 않았습니다 / 시행되지 않았습니다" 처럼 단순히 나열·언급하지 않는다. 실제로 판독된 소견만 서술한다. (이 영상 검사 자체가 통째로 없을 때에만 고정 문구 한 줄.) 단, 판독 소견에 이상·의심이 있어 추가 확인이 필요하면 "정밀 확인을 위해 ○○ 검사를 추가로 권장드립니다" 같은 후속 검사 권고는 오히려 좋다.',
    '- **"비정상적인 구조가 관찰되었습니다" 같은 막연한 표현은 절대 금지.** 이상이 있으면 「어느 구조가 / 어떻게 / 그래서 무슨 의미」를 구체적으로 쓰고, 그렇게 구체적으로 쓸 근거가 없으면 이상을 단정하지 말고 정상 소견 또는 고정 문구를 쓴다.',
    '- 수치(VHS, 심흉비 등)를 적을 때는 **그 수치가 무엇을 재는지·정상인지 이상인지·그래서 무슨 의미인지**를 반드시 함께 쓴다. (나쁜 예: "VHS는 7로 측정되었습니다." / 좋은 예: "심장 크기를 나타내는 VHS가 7로 측정되어 심장이 비대해지지 않은 정상 범위로 확인되었습니다.")',
    `- 글자 수 제한: 최대 ${imgMax}자`,
  ];
  return [...modality('방사선 검사', 'hp5_rad_interp'), '', ...modality('초음파 검사', 'hp5_us_interp')];
}

/** 혈액검사 해석 블록. 전체/섹션 공유. */
export function healthCheckupLabInterpretationLines(): string[] {
  return [
    '========== 혈액검사 해석 (labInterpretation) ==========',
    '- 혈액검사 결과 페이지 상단에 들어가는 전체 해석 요약이다.',
    '- 임상적으로 의미 있는 이상 소견을 중심으로 보호자가 이해할 수 있도록 해석한다.',
    '- 정상 범위를 벗어난 항목들의 의미, 관련 질환 가능성, 주의사항 등을 간결하게 설명한다.',
    '- **수치를 언급할 때마다 숫자만 나열하지 말고, 그 값이 정상인지·이상인지와 무슨 의미인지를 반드시 짧게 함께 쓴다.**',
    '- 모든 수치가 정상이면 정상임을 알리되, 특히 유의할 점이 있으면 짧게 언급한다.',
    '- **안 한 검사는 언급하지 않는다**: 이번 검진에 포함되지 않은 검사·항목을 "○○는 이번 검진에 포함되지 않았습니다" 처럼 단순히 나열·언급하지 않는다. 실제로 시행된 검사 결과만 해석한다. (혈액검사 자체가 통째로 없을 때에만 고정 문구.) 단, 결과에 이상 소견이 있어 추가 확인이 필요하면 "정밀 검사를 위해 ○○ 검사를 추가로 권장드립니다" 같은 후속 검사 권고는 오히려 좋다.',
    `- 글자 수 제한: 공백 포함 최대 ${HEALTH_CHECKUP_PROMPT_LAB_INTERP_MAX_CHARS}자`,
  ];
}

/** 권장 재검진 규칙. 전체/섹션 공유. (헤더는 호출부에서) */
export function healthCheckupRecheckRuleLines(): string[] {
  const t = HEALTH_CHECKUP_MAX_RECHECK_TITLE_CHARS;
  const b = HEALTH_CHECKUP_MAX_RECHECK_BODY_CHARS;
  return [
    '- 사후 관리에서 언급한 내용을 실제 액션 아이템으로 설명하는 부분이다.',
    '- JSON 필드 recheckWithin1to2Weeks, recheckWithin1Month, recheckWithin3Months, recheckWithin6Months 각각에 **한 문자열**로 넣는다.',
    `- 형식: 첫 줄에 제목만(공백 포함 최대 ${t}자). 줄바꿈(\\n) 한 번 후 둘째 줄에 본문만(공백 포함 최대 ${b}자, 본문 안에 추가 줄바꿈 없음).`,
    '- 제목과 본문을 모두 작성한다. 제목 없는 본문 단독 형식은 허용하지 않는다.',
    '- **해당 시기에 실제로 권장할 재검·방문·조치가 없으면 빈 문자열("")을 출력한다. 없는데 억지로 채우지 않는다.**',
    '- ★ 단, 이건 **네 칸을 억지로 채우지 말라는 뜻이지, 있는 내용을 빠뜨려도 된다는 뜻이 아니다.** 종합소견·사후관리에 적힌 재검·재방문·추적관찰·관리 항목은 **하나도 빠짐없이** 네 시기 칸 중 가장 적절한 한 곳에 액션으로 배치한다. (같은 항목을 여러 칸에 중복해 넣지는 않는다.)',
    '- 따라서 **사후관리에 해야 할 일이 적혀 있는데 네 칸이 전부 비거나 한 칸만 채워지는 것은 앞뒤가 맞지 않는다.** 시기를 특정하기 애매한 항목은 버리지 말고, 임상적으로 가장 그럴듯한 시기 칸에 넣는다(급한 것일수록 앞 칸).',
    '- 반대로 종합소견·사후관리에 아무 조치도 없는 건강한 검진이면 네 칸이 모두 비어도 정상이다. **판단 기준은 "칸을 채웠는지"가 아니라 "써야 할 내용을 빠뜨리지 않았는지"다.**',
    '',
    `*recheckWithin1to2Weeks* — 제목 최대 ${t}자, 본문 최대 ${b}자: 가장 단기적인 관점의 액션 아이템을 짧은 명사구 제목 + 간단한 문장 본문으로.`,
    `*recheckWithin1Month* — 제목 최대 ${t}자, 본문 최대 ${b}자: 아주 급하진 않지만 1달 내로 꼭 해야 하는 액션 아이템.`,
    `*recheckWithin3Months* — 제목 최대 ${t}자, 본문 최대 ${b}자: 급하진 않지만 3달 내로 한 번쯤 짚어야 할 액션 아이템.`,
    `*recheckWithin6Months* — 제목 최대 ${t}자, 본문 최대 ${b}자: 중장기 관리를 위한 액션 아이템.`,
  ];
}

/**
 * 재생성 지시(admin 이 '다시 생성' 시 입력) 프롬프트 블록.
 * 직전 초안의 어떤 점이 마음에 들지 않았는지를 알려주는 것이라 mustInclude 보다 우선한다.
 */
export function healthCheckupRevisionLines(revisionNote: string): string[] {
  const note = revisionNote.trim();
  if (!note) return [];
  return [
    '========== 이번 재생성에서 반드시 반영할 수정 요청 (최우선) ==========',
    '아래는 담당자가 **직전 초안을 보고** 고쳐 달라고 요청한 사항이야.',
    '이번 결과물에서는 이 요청을 **최우선으로 반영**해야 해. 다른 규칙과 충돌하지 않는 한 요청을 따른다.',
    '(단, 근거 없는 내용을 지어내라는 뜻은 아니야 — 차트·검사 데이터에 근거가 없으면 억지로 쓰지 말 것)',
    '',
    note,
    '',
  ];
}

export function buildHealthCheckupInstructionBody(opts: {
  programPrefixForPhrase: string;
  excludedAreaExactPhrase: string;
  checkupDate?: string;
  mustInclude?: string;
  /** admin 이 '다시 생성' 시 입력한 수정 요청(직전 초안 대비). */
  revisionNote?: string;
  /** 1 = 종합소견·사후관리·재검진만 (2단계 생성 1단계용). 미지정 시 전체 섹션. */
  outputStage?: 1;
}): string {
  const maxOverall = HEALTH_CHECKUP_PROMPT_MAX_OVERALL_CHARS;
  const maxFollow = HEALTH_CHECKUP_PROMPT_MAX_FOLLOW_UP_CHARS;
  const minOverall = HEALTH_CHECKUP_PROMPT_MIN_OVERALL_CHARS;
  const minFollow = HEALTH_CHECKUP_PROMPT_MIN_FOLLOW_UP_CHARS;
  const pDx = HEALTH_CHECKUP_PROMPT_SYSTEMS_DX_MAX_CHARS;
  const pImp = HEALTH_CHECKUP_PROMPT_SYSTEMS_IMP_MAX_CHARS;
  const pDxDentalSkin = HEALTH_CHECKUP_PROMPT_DENTAL_SKIN_DX_MAX_CHARS;
  const pImpDentalSkin = HEALTH_CHECKUP_PROMPT_DENTAL_SKIN_IMP_MAX_CHARS;
  const pImg = HEALTH_CHECKUP_PROMPT_IMAGING_INTERP_MAX_CHARS;
  const pLab = HEALTH_CHECKUP_PROMPT_LAB_INTERP_MAX_CHARS;
  const { programPrefixForPhrase, excludedAreaExactPhrase, outputStage } = opts;
  const systemsKeyLines = HEALTH_CHECKUP_SYSTEMS_LLM_FIELD_KEYS.map((k) => `- ${k}`).join('\n');
  const { checkupDate, mustInclude, revisionNote } = opts;

  return [
    ...HEALTH_CHECKUP_GROUNDING_LINES,
    '',
    ...healthCheckupRevisionLines(revisionNote ?? ''),
    ...(checkupDate
      ? [
          '========== 검진일자 데이터 필터링 (매우 중요) ==========',
          `이번 보고서의 검진일자는 **${checkupDate}** 이야.`,
          '아래 「참고 데이터」에는 이미 이 날짜의 차트·검사 수치·영상 요약·접종 기록만 포함되어 있어.',
          '만약 다른 날짜의 기록이 섞여 있더라도 **절대로 참고하지 말고**, 오직 이 날짜에 해당하는 발췌만을 근거로 보고서를 작성해줘.',
          '',
        ]
      : []),
    ...(mustInclude
      ? [
          '========== 반드시 포함·강조해야 하는 내용 (매우 중요) ==========',
          '아래 내용은 담당 수의사가 보고서에 **반드시 포함하거나 특별히 강조**하길 원하는 사항이야.',
          '종합 소견·사후 관리·장기·검사 칸 등 가장 적합한 위치에 **빠짐없이 반영**해서 써줘.',
          '단, 전체 톤·형식·글자 수 규칙은 그대로 지켜야 해.',
          '',
          mustInclude,
          '',
        ]
      : []),
    '========== 전체 배경 설명 ==========',
    '너는 세계에서 가장 뛰어난 수의사야.',
    '그리고 이번에 우리가 할 일은 강아지 또는 고양이의 건강검진 보고서를 쓰는 일이야.',
    '실제로 차트에서 추출한 데이터를 확인보면 어떤 검사를 진행했는지 알 수가 있고,',
    '또 중간중간에 참고할만한 내용들도 적혀있을거야.',
    '',
    '**[매우 중요] 아래 「참고 데이터」에 제공된 차트 본문·처방/플랜·검사 수치·신체검사·이미지 내용을 빠짐없이 검토하고, 임상적으로 의미 있는 소견은 모두 보고서에 반영해야 한다. 데이터가 있는데 보고서에 언급되지 않는 내용이 생기지 않도록 한다.**',
    '',
    '========== 전반적인 톤앤매너 ==========',
    '모든 문장은 **인쇄되어 보호자에게 전달되는 공식 건강검진 보고서**에 바로 실릴 글이다. 카톡·블로그·구어체가 아닌 **병원 문서 톤**을 유지한다.',
    '',
    '**전문성·정확성**',
    '- 문장은 **간결하되 피상적이지 않게** 쓴다. 가능하면 임상에서 쓰는 **정확한 용어**를 사용하고, 보호자가 처음 듣는 용어는 **같은 문장 또는 바로 다음 문장에서 한 번 짧게 풀어** 쓴다(장황한 교과서식 설명은 피한다).',
    '- 검사·영상·차트에 근거할 때는 **팩트와 해석을 구분**한다. 확인된 소견은 단정적으로 과장하지 않고, 추정·의심·추적 관찰이 필요한 경우에는 「~소견이 있습니다」「~가 의심되어」「~여부를 함께 보겠습니다」처럼 **임상적으로 타당한 완곡 표현**을 쓴다.',
    '- **검사 근거가 아주 명확한 경우가 아니면**, 질환 표현은 기본적으로 「의심됩니다」「추정됩니다」「가능성이 있습니다」 톤으로 작성하고 확정적 단정 표현은 피한다.',
    '- **단정적 병명 확정·예후 단정**은 피한다. 대신 관찰 소견, 해석 가능한 범위, 재평가·재검의 필요성을 **전문가다운 균형**으로 전달한다.',
    '',
    '**근거 제시**',
    '- 수치·검사명을 인용할 때는 **항목명 + 측정값 + 단위**를 함께 적어 신뢰도를 높인다. 불필요한 수치 나열·전 항목 나열은 하지 않고, **임상적으로 의미 있는 핵심 근거**만 골라 쓴다.',
    '- 여러 근거가 있을 때는 **가장 보호자가 알아야 할 순서**(위험도·시급성)에 맞게 배치한다.',
    ...HEALTH_CHECKUP_VALUE_INTERPRETATION_LINES,
    '',
    '**보호자 대상 문체**',
    '- 기본은 **정중한 설명체**: 존댓말을 바탕으로 `~입니다`, `~하였습니다`, `~해 주시면 좋겠습니다`, `~살펴봐 주시기 바랍니다` 등을 **자연스럽게 섞어** 쓴다. 반말·명령조·과한 유행어는 쓰지 않는다.',
    ...HEALTH_CHECKUP_OWNER_TONE_LINES,
    '- **대화체·구어체로 쓰지 않는다.** 입력 데이터에 메모·SOAP 형식의 구어체가 포함되어 있더라도 출력은 반드시 **공식 보고서 문체**를 유지한다.',
    '- **완전한 문장**으로만 쓴다. 문장을 중간에 끊거나, 키워드만 나열하거나, 기호·줄임에만 의존하는 메모체는 금지한다.',
    '- 읽는 사람이 수의학 비전공자임을 전제로 하되, **유아화·과장된 감정 표현**으로 전문성을 희석하지 않는다.',
    '- 약물 관련 서술이 필요할 때는 **특정 제품명·성분명 같은 정확한 약명은 가능한 한 직접 쓰지 말고**, `항생제`, `진통제`, `소염제`, `간보조제`처럼 **약물군(치료 카테고리) 중심**으로 표현한다.',
    '- 포함할 정보는 동일하게 유지하되, 표현은 **한 톤 담백하게** 쓴다. 불필요한 수식·강조 표현(예: "매우", "상당히", "특히나")은 꼭 필요할 때만 최소화한다.',
    '',
    '**병원·주치 맥락**',
    '- 글의 화자는 **해당 환자를 담당한 본원 수의사**로 일관되게 쓴다. 「다른 병원에 가서 상의하세요」「담당 수의사에게 문의」처럼 **타 기관·타 의료진에게 넘기는 듯한 표현**은 넣지 않는다. 같은 뜻은 재방문·본원 연락·추적 검진 등 **본원 맥락**으로 바꾼다.',
    '',
    '**문단·줄바꿈 (모든 텍스트 필드 공통)**',
    '- **어느 필드이든**(`overallSummary`, `followUpCare`, 각 `hp*_dx`·`hp*_imp`·`hp*_interp` 등), **그 필드 문자열 안에서** 새 문단을 시작할 때는 **반드시 빈 줄 한 줄**을 둔다.',
    '- 한 문단 **안**에서는 불필요한 빈 줄을 넣지 않는다.',
    '- **예외**: 권장 재검진 네 필드는 「제목 한 줄 → `\\n` → 본문 한 줄」 형식만 사용한다.',
    '',
    '========== 논리·문단 구분 ==========',
    '- 최대한 논리적으로 글을 정리해서 써줘.',
    '- 논리상 서로 다른 내용일 경우에는 문단으로 구분해줘.',
    '',
    `마지막으로 내가 섹션별로 글자 수 제한을 줄건데 공백 포함해서 글자수 상한·하한을 항상 철저히 지켜줘야해.`,
    '그래야 보고서에 딱 맞게 들어갈거야.',
    '',
    ...(outputStage === 1
      ? [
          '========== 종합소견·사후관리 작성 논리 (매우 중요) ==========',
          '- 이번 1단계에서 종합소견과 사후관리, 권장 재검진을 먼저 확정한다. 이후 2단계에서 각 장기·검사·영상 칸(hp3_*, hp4_*, hp5_*)이 이 내용을 참고해 작성된다.',
          '- 따라서 종합소견·사후관리에는 **차트·검사 수치·이미지에서 발견한 임상적으로 중요한 내용이 계통별 구분 없이 빠짐없이 포함**되어야 한다. 어떤 이상 소견도 종합소견·사후관리에서 누락되면 안 된다.',
          '- overallSummary에서 언급한 질환·소견은 followUpCare에서 빠짐없이 후속 계획을 다뤄야 한다. 두 필드가 서로 모순 없이 이어져야 한다.',
          '',
        ]
      : [
          '========== 종합소견 ↔ 장기·검사 칸 일관성 (매우 중요) ==========',
          '- 응답은 JSON 한 번에 출력하지만, **글을 짤 때의 논리 순서**는 다음과 같이 생각해라. 먼저 아래 각 장기·검사 칸(`hp3_*`, `hp4_*`, `hp5_*`)에 넣을 내용을 근거와 톤을 맞추어 확정한 뒤, 그 결과를 전체적으로 묶은 것이 `overallSummary`(종합 소견)와 `followUpCare`(사후 관리)다.',
          '- **`overallSummary`와 `followUpCare`에는**, 위 장기·검사 칸에 쓴 내용과 **모순되거나 칸에 전혀 없는 새로운 중요 주장을 만들어 넣지 않는다.**',
          '- 장기·검사 칸에서 구체적으로 서술한 내용(이상 소견, 의심, 수치·영상 근거 등)은 **빠짐없이** `overallSummary` 또는 `followUpCare`(또는 둘 다)에서 **다시 한 번 짚어 준다.** 같은 문장을 복붙할 필요는 없고, 요약·재배열 수준이면 된다.',
          '- 「미포함」 고정 문구만 있는 칸은 종합·사후에서 **별도 확장 서술 없이** 전체 맥락과 모순만 없으면 된다.',
          '',
        ]),
    '========== 종합 소견 ==========',
    ...HEALTH_CHECKUP_OVERALL_RULE_LINES,
    `- 글자 수: 공백 포함 **최소 ${minOverall}자 이상, 최대 ${maxOverall}자 이하**`,
    '',
    '========== 사후 관리 ==========',
    ...HEALTH_CHECKUP_FOLLOWUP_RULE_LINES,
    `- 글자 수: 공백 포함 **최소 ${minFollow}자 이상, 최대 ${maxFollow}자 이하**`,
    '',
    '========== 권장 재검진 ==========',
    ...healthCheckupRecheckRuleLines(),
    '',
    ...(outputStage === 1 ? [] : [
      '========== 장기·검사 섹션 공통 규칙 ==========',
      '- 아래 각 섹션의 질환 목록은 **점검용 예시**일 뿐이다. 데이터에서 진단·의심된 항목만 쓰고, 목록에 있다는 이유로 근거 없이 추측해서 쓰지 않는다. 해당 섹션과 연결할 근거가 없으면 고정 문구를 쓴다.',
      '- 각 계통(hp3_*, hp4_*, hp5_*)마다 **주요 진단(_dx)** 과 **시사점(_imp)** 을 쌍으로 작성한다.',
      ...HEALTH_CHECKUP_DX_IMP_GUIDE_LINES,
      '- **모든 관련 검사 결과가 정상이더라도**, 핵심 검사 결과를 인용하여 "이러이러하여 정상이다" 형식으로 써줘. 근거 없는 "정상입니다" 단독 문장은 피한다.',
      `- 주요 진단 최대 ${pDx}자, 시사점 최대 ${pImp}자 (치과/피부: 각 최대 ${pDxDentalSkin}자 / ${pImpDentalSkin}자)`,
      '',
      '========== 질환 소개 박스(3·4p) 규칙 ==========',
      ...HEALTH_CHECKUP_DISEASE_BOX_RULE_LINES,
      '',
      '========== 미포함 고정 문구 규칙 ==========',
      `- 프로그램명: ${programPrefixForPhrase}`,
      `- 이번 검진 패키지에 포함되지 않은 계통·검사로, 차트·검사·이미지를 읽어도 그 섹션과 **직접 연결할 내용이 전혀 없을 때**에는 해당 섹션의 주요 진단·시사점 두 칸 모두에 아래 고정 문구만 넣는다. 앞뒤 공백·추가 문장 일절 금지.`,
      `  → 고정 문구: ${excludedAreaExactPhrase}`,
      '- 방사선·초음파 섹션도 영상 근거가 없으면 동일하게 고정 문구를 사용한다.',
      '',
      ...HEALTH_CHECKUP_ORGAN_ORDER.flatMap((k) => [...healthCheckupOrganBlockLines(k), '']),
      ...healthCheckupImagingBlockLines(),
      '',
      ...healthCheckupLabInterpretationLines(),
      '',
    ]),
    '========== JSON 키 ↔ 인쇄 시트 (영문 키 이름을 정확히 지킬 것) ==========',
    '- overallSummary, followUpCare',
    '- recheckWithin1to2Weeks, recheckWithin1Month, recheckWithin3Months, recheckWithin6Months',
    ...(outputStage === 1 ? [] : [
      '- hp3_circ_dx, hp3_circ_imp, hp3_digest_dx, hp3_digest_imp, hp3_endo_dx, hp3_endo_imp',
      '- hp3_renal_uro_dx, hp3_renal_uro_imp, hp3_hepatobiliary_dx, hp3_hepatobiliary_imp, hp3_msk_dx, hp3_msk_imp',
      '- hp4_dental_dx, hp4_dental_imp, hp4_skin_dx, hp4_skin_imp',
      '- hp5_rad_interp, hp5_us_interp',
      '- (선택) 장기별 확진 질환명 배열(이름만, 문자열 배열): hp3_circ_diseases, hp3_digest_diseases, hp3_endo_diseases, hp3_renal_uro_diseases, hp3_hepatobiliary_diseases, hp3_msk_diseases, hp4_dental_diseases, hp4_skin_diseases',
      '- labInterpretation',
      '',
      '응답 스키마에 포함할 시스템 시트 문자열 키(전부 필수):',
      systemsKeyLines,
    ]),
  ].join('\n');
}
